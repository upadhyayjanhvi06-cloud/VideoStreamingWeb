<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<title>Video Interaction Platform</title>
<link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;500;700&display=swap" rel="stylesheet">

<style>
body {
    margin: 0;
    font-family: 'Poppins', sans-serif;
    background: linear-gradient(135deg, #0f2027, #203a43, #2c5364);
    color: white;
    display: flex;
    justify-content: center;
    padding: 30px;
}

.card {
    background: #111;
    width: 420px;
    border-radius: 16px;
    padding: 20px;
    box-shadow: 0 20px 50px rgba(0,0,0,0.6);
}

video {
    width: 100%;
    border-radius: 12px;
}

.actions {
    display: flex;
    justify-content: space-between;
    margin-top: 15px;
}

button {
    background: none;
    border: none;
    color: white;
    font-size: 22px;
    cursor: pointer;
    position: relative;
}

button:hover {
    transform: scale(1.2);
}

/* Animations */
.heart { color: red; font-size: 40px; position:absolute; animation: pop 0.8s forwards; }
.arrow { color: white; font-size: 35px; position:absolute; animation: fly 1s forwards; }
.thumb { color:#00ffcc; font-size:35px; position:absolute; animation: rise 1s forwards; }

@keyframes pop { 0%{scale:0} 50%{scale:1.4} 100%{opacity:0; scale:2} }
@keyframes fly { to{opacity:0; transform:translateX(120px)} }
@keyframes rise { to{opacity:0; transform:translateY(-80px)} }

/* Extra actions */
.extra-actions {
    display: flex;
    justify-content: space-around;
    margin-top: 10px;
    font-size: 14px;
}

.extra-actions span {
    cursor: pointer;
    color: #ccc;
}

.extra-actions span:hover {
    color: #00ffcc;
}

/* Description */
.description {
    background: #1a1a1a;
    padding: 10px;
    border-radius: 8px;
    margin-top: 15px;
    font-size: 14px;
}

/* Voice Search */
.voice-search {
    margin-top: 15px;
    display: flex;
    gap: 10px;
}

.voice-search input {
    flex: 1;
    padding: 8px;
    border-radius: 6px;
    border: none;
}

/* Comments */
.comments { margin-top: 15px; }
input { width:100%; padding:10px; border-radius:8px; border:none; }
.comment-list { font-size:14px; margin-top:10px; }

/* Recommended videos */
.recommend {
    margin-top: 20px;
}

.recommend h3 {
    margin-bottom: 10px;
}

.video-item {
    background: #1c1c1c;
    padding: 10px;
    border-radius: 8px;
    margin-bottom: 8px;
    cursor: pointer;
}

.video-item:hover {
    background: #2c5364;
}
</style>
</head>

<body>

<div class="card">
    <video controls>
        <source src="sample.mp4" type="video/mp4">
    </video>

    <!-- Like Share Subscribe -->
    <div class="actions">
        <button onclick="like(this)">❤️</button>
        <button onclick="share(this)">🔗</button>
        <button onclick="subscribe(this)">🔔</button>
    </div>

    <!-- Watch later / Favourite -->
    <div class="extra-actions">
        <span onclick="alert('Saved to Watch Later ⏰')">⏰ Watch Later</span>
        <span onclick="alert('Added to Favourites ⭐')">⭐ Favourite</span>
    </div>

    <!-- Description -->
    <div class="description">
        <strong>Description:</strong><br>
        Learn modern web development concepts with interactive UI and animations.
    </div>

    <!-- Voice Search -->
    <div class="voice-search">
        <input type="text" id="voiceInput" placeholder="Voice search video...">
        <button onclick="voiceSearch()">🎙️</button>
    </div>

    <!-- Comments -->
    <div class="comments">
        <input type="text" id="commentInput" placeholder="Add a comment..." onkeypress="addComment(event)">
        <div class="comment-list" id="commentList"></div>
    </div>

    <!-- Recommended -->
    <div class="recommend">
        <h3>▶ Recommended Videos</h3>
        <div class="video-item">JavaScript Animation Tricks</div>
        <div class="video-item">HTML & CSS Full Course</div>
        <div class="video-item">Frontend Project Ideas</div>
        <div class="video-item">Tech Fest Winning Websites</div>
    </div>
</div>

<script>
function like(btn){
    let h=document.createElement("div");h.innerHTML="❤️";h.className="heart";btn.appendChild(h);
    setTimeout(()=>h.remove(),800);
}
function share(btn){
    let a=document.createElement("div");a.innerHTML="➤";a.className="arrow";btn.appendChild(a);
    setTimeout(()=>a.remove(),1000);
}
function subscribe(btn){
    let t=document.createElement("div");t.innerHTML="👍";t.className="thumb";btn.appendChild(t);
    setTimeout(()=>t.remove(),1000);
}
function addComment(e){
    if(e.key==="Enter"){
        let i=commentInput.value.trim();
        if(i){commentList.innerHTML+=`<p>💬 ${i}</p>`;commentInput.value="";}
    }
}

/* Voice search */
function voiceSearch(){
    if(!('webkitSpeechRecognition'in window)){
        alert("Voice search not supported");
        return;
    }
    let rec=new webkitSpeechRecognition();
    rec.lang="en-US";
    rec.start();
    rec.onresult=e=>voiceInput.value=e.results[0][0].transcript;
}
</script>

</body>
</html>